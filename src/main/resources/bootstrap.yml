server:
  port: ${PORT}

spring:
  application:
    name: fbk-api-gateway
  cloud:
    config:
      discovery.enabled: true
      enabled: true
      name: fbk-api-gateway
      uri: ${CONFIG_SERVER_URI}

eureka.client.serviceUrl.defaultZone: ${SERVICE_DISCOVERER_URL}/eureka/
